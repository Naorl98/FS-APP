// watch_ad.js
// This script runs when the ad page loads. Currently no logic is needed.
// If you want to auto-close after 5 seconds, uncomment the code below:

// setTimeout(() => {
//   window.close();
// }, 5000);
